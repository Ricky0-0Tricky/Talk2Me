@model IEnumerable<Talk2Me.Models.Chat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserB)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserA.PasswordHash)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserB.PasswordHash)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
